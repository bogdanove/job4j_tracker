При анализе использовался инструмент JCONSOLE
Настройки для профилирования : -XX:+UseSerialGC -Xmx30m -Xms30m -Xlog:gc:log.txt

По результат запуска в логе видно, что при добавлении Item по одной штуке,
сборщик мусора справляется малыми сборками. Но при добавлении большого количества
переходит на большие сборки, но это не помогает, программа уходит в OutOfMemoryError.

Настройки профилирования : -XX:+UseParallelGC -Xmx30m -Xms30m -Xlog:gc:log.txt

При добавлении по одной штуке прошла только одна малая сборка, но при добавлении большого
количества начались большие сборки мусора и результат аналогичен, OutOfMemoryError.

Настройки профилирования : -XX:+UseG1GC -Xmx50m -Xms50m -Xlog:gc:log.txt

После увеличения хипа, малые сборки прекратились при добавлении по одной штуке.
Но при добавлении большого количества, малые и большие сборки начали чередоваться,
этот сборщик продержался больше всех, но все равно закончилось OutOfMemoryError.